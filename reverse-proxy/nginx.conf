http {
  upstream api {
    ip_hash;
    server api
  }

  upstream static {
    server static
  }

  server {
    listen 80;

    location /api {
      proxy_pass http://api
    }

    location / {
      proxy_pass http://static
    }
  }
}
